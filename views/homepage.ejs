<div ng-app="schoolApp">
  <div ng-controller="PeopleController">
    <h1>{{pageTitle}}</h1>
    <li ng-repeat="person in people">{{person.name}}</li>


    <input placeholder="Add a user" ng-model="add_name"><button ng-click="postTheData()">Submit</submit>
  </div>
</div>






<!--
<div ng-app>
  <input ng-model="name">

  <h1>Hello {{name}}</h1>
</div> -->
